#[derive(Debug, Clone)]
pub enum Code {
    Constant(usize),
    Echo,
    Add,
    Subtract,
    Multiply,
    Divide,
    Concat,
    LessThan,
    GreaterThan,
    Assign(String),
    AssignToIndex,
    Get(String),
    GetConstant(String),
    Jump(usize),
    JumpIfFalse(usize),
    JumpIfTrue(usize),
    Pop,
    True,
    False,
    Null,
    Return,
    ReturnWith,
    InitArray,
    AddToArray,
    GetArrayItem,
    InitCall(String),
    DoUserCall,
    DoInternalCall,
    SendArg,
}